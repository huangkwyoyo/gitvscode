# 风险模式

## MySQL 阶段风险

- 结果表无主键或唯一业务键。
- 业务 SQL 未限制业务日期。
- 使用 `SELECT *`。
- Join 条件缺失或不完整。
- 指标除法未处理除零。
- 校验 SQL 不输出标准校验字段。

## Doris 转换阶段风险

- MySQL 特有函数无法直接映射。
- `TEXT` 等类型需要转换。
- 宽表字段过多导致建表策略需要人工确认。
- 大表导入和聚合可能触发资源风险。
