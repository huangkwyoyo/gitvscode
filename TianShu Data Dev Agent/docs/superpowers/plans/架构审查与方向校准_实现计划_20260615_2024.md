# 架构审查与方向校准 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 执行路线 A（保守演进）——文档化 LLM 边界、补充设计决策记录、建立 IR 层级说明、拆分 layer3_plan.py 的 IR 定义与规划逻辑

**Architecture:** 不改架构骨架。在文档层面补全 LLM 防火墙证据链和设计决策记录；在代码层面将 layer3_plan.py（948 行）拆分为 layer3_ir.py（纯 IR 定义）和 layer3_plan.py（规划逻辑），更新 10 个文件的 import 路径

**Tech Stack:** Python 3.x, duckdb, pytest

---

### Task 1: LLM 边界文档化

**Files:**
- Modify: `D:\Program Files\gitvscode\TianShu Data Dev Agent\AGENTS.md:110-117`
- Modify: `D:\Program Files\gitvscode\TianShu Data Dev Agent\scripts\pipeline\layer3_plan.py:1-18`

- [ ] **Step 1: AGENTS.md §3.1 表格新增"代码证据"列**

将 §3.1 的表格从 3 列扩展为 4 列：

```markdown
### 3.1 LLM 不能做什么

| 禁止 | 原因 | 替代机制 | 代码证据 |
|------|------|---------|---------|
| 生成 SQL 文本 | SQL 必须可审计、可追溯 | ColumnBindingTable + 模板编译器 | `layer4_generate.py`——compile_sql() 纯模板编译，无 LLM 调用 |
| 推荐表名 | 表选择是确定性决策 | `resolve_layer()` 函数 | `layer3_plan.py:441`——_resolve_layer() 查 ColumnBindingTable |
| 决定 JOIN 条件 | JOIN 必须来自白名单 | JoinGraph 数据结构 | `column_binding.py:JOIN_WHITELIST`——硬编码白名单列表 |
| 映射 metric → column | 映射必须唯一确定 | ColumnBindingTable | `column_binding.py:get_binding_by_metric_name()`——纯函数查表 |
| 编造字段名 | 违背零幻觉原则 | 所有字段来自 meta.metric_definitions | `column_binding.py:METRIC_BINDINGS`——BindingEntry 列表 |
| 判断 G3 vs G2 | 层级选择是规则决策 | `resolve_layer()` 函数 | `layer3_plan.py:441`——纯 if/else 逻辑 |
| 绕过安全策略 | 安全是不可协商的 | Layer 5 纯规则引擎 | `layer5_validate.py:validate_sql()`——6 项硬检查 |
```

- [ ] **Step 2: layer3_plan.py 文件头强化 LLM 硬边界注释**

将当前文件头（第 1-18 行）替换为：

```python
"""
Layer 3：SQLPlan 规划层

职责：
  1. 接收 Layer 2 的 Intent 对象
  2. 通过查 ColumnBindingTable 确定每个指标的物理列
  3. 决定数据源层级（G3 优先 → G2 降级）
  4. 构造 JoinGraph（单表或多表）
  5. 注入执行约束
  6. 输出完整 SQLPlan 对象

===== LLM 硬边界 =====

此层及以下所有层（L3-L8）**完全禁止 LLM 参与**。
LLM 输出的 Intent 对象不包含任何表名、列名、JOIN 条件。
所有物理层决策（表选择、JOIN 构造、列绑定）由本层纯代码完成。

安全保证：即使 LLM 产生幻觉（输出错误的指标名），
最坏结果也是本层查 ColumnBindingTable 失败 → BLOCKED（拒绝执行），
不会生成错误的 SQL。因为：
  - 表名来自 ColumnBindingTable（代码查表，非 LLM 选择）
  - JOIN 路径来自 JOIN_WHITELIST（硬编码白名单，非 LLM 构造）
  - 列绑定由 get_binding_by_metric_name() 纯函数完成

LLM 角色：
  **完全禁止**。此层是纯确定性代码。
  所有决策通过查表和规则完成，不经过任何 LLM 调用。

输入：Intent 对象
输出：SQLPlan 对象
"""
```

- [ ] **Step 3: 提交**

```bash
cd "D:\Program Files\gitvscode"
git add "TianShu Data Dev Agent/AGENTS.md" "TianShu Data Dev Agent/scripts/pipeline/layer3_plan.py"
git commit -m "docs: LLM 边界文档化——AGENTS.md 新增代码证据列 + layer3_plan.py 强化硬边界注释"
```

---

### Task 2: 设计决策记录 + IR 层级说明 + 已知问题降级

**Files:**
- Modify: `D:\Program Files\gitvscode\TianShu Data Dev Agent\PROJECT_STATUS.md:175-245`

- [ ] **Step 1: §六新增"决策 7：为什么 8 层"**

在 §六 末尾（决策 6 之后，`---` 之前）插入：

```markdown
### 决策 7：为什么 8 层而非 3 层（2026-06-15）
- **决策**：Data Dev Agent 使用 8 层管道而非 Text2SQL Agent 的 3 层 IR 架构
- **原因**：两个系统定位不同——批量数据生产需要每层独立可审计、可重试、可观测。

#### 每层的不可合并理由

| 层 | 职责 | 为什么不能合并 |
|----|------|--------------|
| L1 需求解析 | YAML Schema 校验 | L1 是规则解析 YAML，L2 可能调用 LLM。合并后 LLM 边界模糊 |
| L2 意图理解 | 指标消歧 + LLM 翻译 | LLM 的最后一个接触点——后续全部确定性 |
| L3 SQLPlan 规划 | 查表 + JOIN 构造 | LLM 防火墙——合并到 L2 会让 LLM 接触物理层 |
| L4 SQL 编译 | 多方言模板编译 | 与 L3 分离可独立测试方言——不影响规划决策 |
| L5 SQL 校验 | 6 项安全检查 | 独立审计——校验失败需追溯到 L3 的 SQLPlan，不能混在编译层 |
| L6 SQL 执行 | 只读执行 + 重试 | 与 L7 分离——评估可能标记 DIRTY 但仍需输出产物 |
| L7 结果评估 | 统计质量检查 | 生产独有——交互查询不需要空值率/行数范围检查 |
| L8 产物发布 | 文件输出 + 审计报告 | 生产独有——交互查询输出自然语言，不需要 Parquet |

#### 与 Text2SQL Agent 3 层 IR 的对比

| | Text2SQL Agent（3 IR） | Data Dev Agent（8 层） |
|---|---|---|
| 场景 | 用户问 → 几秒内回答 | YAML → 定时批量产出文件 |
| 输出 | 中文自然语言回答 | Parquet/CSV + 审计报告 + 调度配置 |
| 失败处理 | 反问/拒绝即可 | 需定位到具体哪一层失败 |
| LLM 接触面 | 每次查询都经过 LLM（L1 + L2） | LLM 仅首次解析时参与（L1-L2） |
| 审计要求 | 中等 | 高——每层独立校验记录 |

**结论**：8 层是批量生产管道的最小必要层数。Text2SQL 的 3 层对交互查询正确，但它若做批量生产也需补上校验/评估/发布层。
```

- [ ] **Step 2: §三扩展为完整 IR 层级说明**

将 §三"IR 成熟度"表格替换为完整 IR 层级说明：

```markdown
### 3.3 IR 成熟度

#### IR 信息隔离链

每个 IR 只暴露下一层需要的信息——这是"信息防火墙"设计：

```
Requirement IR     → 隔离 YAML 语法细节（下层不需要知道输入是 YAML）
Intent IR          → 隔离用户原始措辞（LLM 防火墙在此——下层不接触 LLM 输出）
SQLPlan IR         → 隔离指标→列的映射逻辑（下层不关心 G3 vs G2 决策）
ExpressionRef IR   → 隔离表达式树结构（嵌入 SQLPlan，编译器只看到标准化 IR）
WindowFunctionDef  → 隔离窗口函数语义（嵌入 SQLPlan，方言无关）
CTEDefinition      → 隔离 CTE 链结构（嵌入 SQLPlan，递归深度可静态检查）
SQL 文本            → 隔离方言特定语法（执行层不关心 SQL 怎么生成的）
ExecutionResult    → 隔离数据库连接细节（评估层不关心用什么引擎执行）
Product 文件        → 隔离 Parquet/CSV 内部格式（消费者只拿到文件）
```

#### 每个 IR 为什么存在

| IR 类型 | 解决什么问题 | 没有它会怎样 |
|--------|-------------|-------------|
| Requirement | YAML 解析和 Schema 校验分离 | L2 需要直接处理 YAML，LLM 边界被污染 |
| Intent | LLM 输出和工程代码之间的防火墙 | LLM 可能输出表名，安全边界消失 |
| SQLPlan | 规划决策（G3/G2/JOIN）的标准化表达 | L4 编译器需要自己做规划决策，耦合 |
| ExpressionRef | 表达式从字符串变为结构化 IR | 编译器需要解析字符串表达式，易出错 |
| WindowFunctionDef | 窗口函数的方言无关表达 | 每种方言需要自己解析窗口函数语义 |
| CTEDefinition | CTE 链的显式建模 | WITH 子句的递归引用无法校验 |

#### IR 类型状态

| IR 类型 | 状态 | 冻结 |
|---------|------|------|
| Intent IR | ✅ 稳定 | 字段结构已锁定 |
| SQLPlan IR | ✅ 已冻结 | C1-C6 修正完成 |
| PipelinePlan IR | ✅ 稳定 | DAG 结构确定 |
| ExpressionRef IR | ✅ 已冻结 | 10 类型 + 3 方言 |
```

- [ ] **Step 3: §七"下一步计划"更新为当前实际状态**

```markdown
## 七、下一步计划

### 短期（P0——质量加固）
- `layer3_plan.py` 拆分为 `layer3_ir.py` + `layer3_plan.py`（进行中）
- 架构审查文档补充——LLM 边界、设计决策记录、IR 层级说明（进行中）

### 中期（P1——生产化就绪）
- 完整 DAG 端到端测试——5 步管道 + 中间表管理
- KEY_MERGE 增量策略实现
- Prompt 回归系统正式运行（`regression_baseline.yml` 基线）

### 长期（P2——生态扩展）
- 多环境部署（开发/测试/生产 DuckDB 路径切换）
- CI/CD 门禁接入——pre-commit hook + GitHub Actions
- 事故高发区域监测——5 阶段管道完整执行 + 异常告警
```

- [ ] **Step 4: §八已知问题——降级 ColumnBindingTable 加载问题**

将"已知问题"表中第 2 行状态从"已缓解——可追踪加载来源"改为"已缓解——P1-1 修复已添加 `get_load_status()` 诊断函数，可实时查询事实源加载状态和漂移风险"。

- [ ] **Step 5: 提交**

```bash
cd "D:\Program Files\gitvscode"
git add "TianShu Data Dev Agent/PROJECT_STATUS.md"
git commit -m "docs: PROJECT_STATUS 新增决策7/IR层级说明，更新路线图，降级已知问题"
```

---

### Task 3: 拆分 layer3_plan.py → layer3_ir.py + layer3_plan.py

**Files:**
- Create: `D:\Program Files\gitvscode\TianShu Data Dev Agent\scripts\pipeline\layer3_ir.py`
- Modify: `D:\Program Files\gitvscode\TianShu Data Dev Agent\scripts\pipeline\layer3_plan.py`

- [ ] **Step 1: 创建 layer3_ir.py——复制 IR 定义部分**

将 `layer3_plan.py` 第 1-427 行（从文件头到 `SQLPlan` dataclass 结束）复制到新文件 `layer3_ir.py`，并修改文件头：

```python
"""
Layer 3 IR 定义——SQLPlan 相关的所有数据结构和枚举类型

此模块仅包含纯 IR 定义（dataclass + Enum），不包含任何规划逻辑。
规划逻辑位于 layer3_plan.py。

IR 类型：
  - SQLPlan / PipelinePlan（顶层计划 IR）
  - JoinGraph / JoinNode / JoinCondition（表结构）
  - ColumnBinding / DimensionColumnBinding / FilterBinding（列绑定）
  - ExpressionRef / ExpressionOperand / ExpressionConfig（表达式）
  - WindowFunctionDef / WindowFunctionArg（窗口函数）
  - CTEDefinition（CTE）
  - ColumnRef / LiteralValue / OrderByEntry（通用结构）
  - ExecutionConstraints（执行约束）
  - 所有枚举类型（ExpressionType / WindowFunctionName / FilterType 等）

异常：
  - SQLCompileError——SQL 编译错误（跨层共享）
"""

from __future__ import annotations

from dataclasses import dataclass, field
from enum import Enum
from typing import Optional, Any
```

然后粘贴 `layer3_plan.py` 第 38-427 行的所有内容（从 `class SQLCompileError` 到 `SQLPlan` dataclass 结束）。

`layer3_plan.py` 第 27-35 行的 import（`from .column_binding import ...` 和 `from .layer2_intent import ...`）在 layer3_ir.py 中不需要——IR 定义不依赖这些。但如果某些 IR 类型（如 `SQLPlan`）import 了外部类型，需要检查。

实际上检查后发现：IR 定义中的 import 只有 `from dataclasses import dataclass, field`、`from enum import Enum`、`from typing import Optional, Any`、`from datetime import datetime`（用于 ExecutionConstraints。哦等等，ExecutionConstraints 不需要 datetime。`from datetime import datetime` 只在 `_generate_plan_id()` 函数中使用，那是规划逻辑）。

所以 layer3_ir.py 的 import 是干净的，不需要 column_binding 或 layer2_intent。

- [ ] **Step 2: 修改 layer3_plan.py——移除 IR 定义，添加 import**

删除 `layer3_plan.py` 第 20-427 行（import 和所有 IR 定义），保留第 1-18 行（文件头注释），然后添加：

```python
from .layer3_ir import (
    # 异常
    SQLCompileError,
    # 核心 IR
    SQLPlan,
    JoinGraph, JoinNode, JoinCondition,
    ColumnBinding, DimensionColumnBinding, FilterBinding,
    # 表达式 IR
    ExpressionRef, ExpressionOperand, ExpressionConfig,
    ExpressionType, OperandKind,
    ColumnRef, LiteralValue, LiteralType, OutputType,
    # 窗口函数 IR
    WindowFunctionDef, WindowFunctionName, WindowFunctionArg,
    FunctionArgKind, OrderByEntry, FrameType,
    # CTE IR
    CTEDefinition,
    # 过滤器
    FilterType,
    # 执行约束
    ExecutionConstraints,
)
```

然后保留第 428 行之后的所有规划逻辑代码（`_generate_plan_id`、`_resolve_layer` 等）。

- [ ] **Step 3: 验证 layer3_plan.py 内部一致性**

运行快速检查——`construct_sqlplan` 和所有辅助函数仍然能找到它们需要的类型：

```bash
cd "D:\Program Files\gitvscode\TianShu Data Dev Agent"
python -c "from scripts.pipeline.layer3_plan import construct_sqlplan, SQLPlan; print('OK')"
```

预期输出：`OK`

- [ ] **Step 4: 提交**

```bash
cd "D:\Program Files\gitvscode"
git add "TianShu Data Dev Agent/scripts/pipeline/layer3_ir.py" "TianShu Data Dev Agent/scripts/pipeline/layer3_plan.py"
git commit -m "refactor: 拆分 layer3_plan.py——IR 定义移至 layer3_ir.py"
```

---

### Task 4: 更新所有 import 路径

**Files:**
- Modify: `D:\Program Files\gitvscode\TianShu Data Dev Agent\scripts\pipeline\layer3_pipeline_plan.py:27`
- Modify: `D:\Program Files\gitvscode\TianShu Data Dev Agent\scripts\pipeline\layer4_generate.py:24-28`
- Modify: `D:\Program Files\gitvscode\TianShu Data Dev Agent\scripts\pipeline\layer4_expression.py:26-36`
- Modify: `D:\Program Files\gitvscode\TianShu Data Dev Agent\scripts\pipeline\layer4_window.py:25`
- Modify: `D:\Program Files\gitvscode\TianShu Data Dev Agent\scripts\pipeline\layer4_cte.py:25-29`
- Modify: `D:\Program Files\gitvscode\TianShu Data Dev Agent\scripts\pipeline\layer4_operation.py:32`
- Modify: `D:\Program Files\gitvscode\TianShu Data Dev Agent\scripts\pipeline\layer5_validate.py:23`
- Modify: `D:\Program Files\gitvscode\TianShu Data Dev Agent\scripts\pipeline\layer5_validate_pipeline.py:36`
- Modify: `D:\Program Files\gitvscode\TianShu Data Dev Agent\scripts\pipeline\layer7_evaluate.py:25`
- Modify: `D:\Program Files\gitvscode\TianShu Data Dev Agent\scripts\pipeline\layer8_product.py:26`
- Modify: `D:\Program Files\gitvscode\TianShu Data Dev Agent\scripts\pipeline\run_pipeline.py:59-60`
- Modify: `D:\Program Files\gitvscode\TianShu Data Dev Agent\scripts\quality\check_pipeline.py:255`
- Modify: `D:\Program Files\gitvscode\TianShu Data Dev Agent\tests\test_pipeline.py:19-33`
- Modify: `D:\Program Files\gitvscode\TianShu Data Dev Agent\tests\test_pipeline_phase2.py:23-29`

> **策略**：只更新只导入 IR 类型的文件。`run_pipeline.py` 和 `check_pipeline.py` 同时需要 `construct_sqlplan`（来自 `layer3_plan`）和 IR 类型（现在来自 `layer3_ir`），需要拆分为两行 import。

- [ ] **Step 1: 更新只导入 IR 类型的文件（10 个文件）**

将以下文件中的 `from .layer3_plan import ...` 改为 `from .layer3_ir import ...`，保持导入内容不变：

```
layer3_pipeline_plan.py:27   → from .layer3_ir import SQLPlan
layer4_generate.py:24-28     → from .layer3_ir import (SQLPlan, JoinGraph, JoinNode, ColumnBinding, FilterBinding, DimensionColumnBinding, FilterType, SQLCompileError)
layer4_expression.py:26-36   → from .layer3_ir import (ExpressionRef, ExpressionOperand, ExpressionConfig, ExpressionType, OperandKind, ColumnRef, LiteralValue, LiteralType, JoinGraph, SQLCompileError)
layer4_window.py:25          → from .layer3_ir import (...)
layer4_cte.py:25-29          → from .layer3_ir import (CTEDefinition, SQLPlan, SQLCompileError)
layer4_operation.py:32       → from .layer3_ir import SQLCompileError
layer5_validate.py:23        → from .layer3_ir import SQLPlan
layer5_validate_pipeline.py:36 → from .layer3_ir import SQLPlan
layer7_evaluate.py:25        → from .layer3_ir import SQLPlan
layer8_product.py:26         → from .layer3_ir import SQLPlan
```

每个文件的修改方式：只改 `layer3_plan` → `layer3_ir`，导入的符号名称不变。

- [ ] **Step 2: 更新同时需要 IR 类型和规划函数的文件（3 个文件）**

**run_pipeline.py:59-60**——拆分为两行：

```python
from scripts.pipeline.layer3_ir import SQLPlan
from scripts.pipeline.layer3_plan import construct_sqlplan
```

**check_pipeline.py:255**——拆分为两行（在函数内部 import）：

```python
from scripts.pipeline.layer3_ir import SQLPlan
from scripts.pipeline.layer3_plan import construct_sqlplan
```

**test_pipeline.py:19-33**——拆分为两段 import：

```python
from scripts.pipeline.layer3_ir import (
    SQLPlan, SQLCompileError,
    JoinGraph, JoinNode, JoinCondition,
    ColumnBinding, FilterBinding,
    # Phase 2 类型
    ExpressionRef, ExpressionOperand, ExpressionConfig,
    ExpressionType, OperandKind,
    ColumnRef, LiteralValue, LiteralType, OutputType,
    FilterType,
    # Phase 4 类型
    WindowFunctionDef, WindowFunctionName, WindowFunctionArg,
    FunctionArgKind, OrderByEntry, FrameType,
    # Phase 5 类型
    CTEDefinition,
)
from scripts.pipeline.layer3_plan import construct_sqlplan
```

同时更新 test_pipeline.py 第 197 行的内联 import：
```python
from scripts.pipeline.layer3_ir import SQLPlan
```

以及第 1472、1534、1623 行的内联 import：
```python
from scripts.pipeline.layer3_ir import FilterType
```

**test_pipeline_phase2.py:23-29**——同 test_pipeline.py 拆分：

```python
from scripts.pipeline.layer3_ir import (
    SQLPlan,
    JoinGraph,
    JoinNode,
    JoinCondition,
    ColumnRef,
)
```

- [ ] **Step 3: 提交**

```bash
cd "D:\Program Files\gitvscode"
git add "TianShu Data Dev Agent/scripts/pipeline/layer3_pipeline_plan.py"
git add "TianShu Data Dev Agent/scripts/pipeline/layer4_generate.py"
git add "TianShu Data Dev Agent/scripts/pipeline/layer4_expression.py"
git add "TianShu Data Dev Agent/scripts/pipeline/layer4_window.py"
git add "TianShu Data Dev Agent/scripts/pipeline/layer4_cte.py"
git add "TianShu Data Dev Agent/scripts/pipeline/layer4_operation.py"
git add "TianShu Data Dev Agent/scripts/pipeline/layer5_validate.py"
git add "TianShu Data Dev Agent/scripts/pipeline/layer5_validate_pipeline.py"
git add "TianShu Data Dev Agent/scripts/pipeline/layer7_evaluate.py"
git add "TianShu Data Dev Agent/scripts/pipeline/layer8_product.py"
git add "TianShu Data Dev Agent/scripts/pipeline/run_pipeline.py"
git add "TianShu Data Dev Agent/scripts/quality/check_pipeline.py"
git add "TianShu Data Dev Agent/tests/test_pipeline.py"
git add "TianShu Data Dev Agent/tests/test_pipeline_phase2.py"
git commit -m "refactor: 更新 import 路径——layer3_plan → layer3_ir（纯 IR 类型导入）"
```

---

### Task 5: 全面验证

**Files:** 无新建或修改——纯验证步骤

- [ ] **Step 1: 运行全部测试**

```bash
cd "D:\Program Files\gitvscode\TianShu Data Dev Agent"
python -m pytest tests/ -v
```

预期：**135 passed**，0 failed

如果出现 import 错误，检查对应文件的 import 路径是否正确更新。

- [ ] **Step 2: 运行 quality check（12 项）**

```bash
cd "D:\Program Files\gitvscode\TianShu Data Dev Agent"
python scripts/quality/check_pipeline.py --verbose
```

预期：**所有检查通过**（12/12，包含 SQL 编译完整性——这验证了 import 路径拆分不影响全链路 dry-run）

- [ ] **Step 3: 运行全链路 dry-run**

```bash
cd "D:\Program Files\gitvscode\TianShu Data Dev Agent"
python scripts/pipeline/run_pipeline.py -r fixtures/requirements/trip_daily_report.yml --dry-run
```

预期：正常输出 SQL 文本，无异常

- [ ] **Step 4: 运行全链路 dry-run（其他 fixture）**

```bash
cd "D:\Program Files\gitvscode\TianShu Data Dev Agent"
python scripts/pipeline/run_pipeline.py -r fixtures/requirements/parking_daily_report.yml --dry-run
python scripts/pipeline/run_pipeline.py -r fixtures/requirements/crash_daily_report.yml --dry-run
```

预期：均正常输出 SQL 文本

- [ ] **Step 5: 提交（如果前面有修正）**

如果有任何修正，提交并推送。否则直接推送前面的提交：

```bash
cd "D:\Program Files\gitvscode"
git push origin main
```

---

### Task 6: Obsidian 架构设计文档更新

**Files:**
- Modify: `C:\Users\62414\Nutstore\1\Obsidian Vault\Ai Learning\Data Dev Agent知识积累\DataDevAgent完整架构设计_20260614_1730.md`

- [ ] **Step 1: 在 Obsidian 文档中新增"LLM 防火墙"专题**

找到合适的插入位置（§3 架构设计部分末尾，§3.12 安全审计之后），新增：

```markdown
---

### §3.13 LLM 防火墙——信息隔离边界（2026-06-15）

**核心原则**：LLM 不接触物理层信息。L2（意图理解）是 LLM 的最后接触点。

#### 信息分界图

```

LLM 可见（Intent IR 中的字段）     LLM 不可见（被 L3 代码隔离）
─────────────────────────────    ─────────────────────────────
metrics: ["trip_count"]          表名: gold.dws_daily_trip_summary
dimensions: ["date"]             列名: trip_count, trip_date  
filters: {date_range: [...]}     JOIN 条件: dim_date.date = fact.trip_date
                                 数据源策略: G3_DIRECT vs G2_FACT
                                 方言选择: DuckDB vs Hive vs PostgreSQL
                                 聚合表达式: SUM(trip_count)
```

#### 安全保证机制

1. **LLM 输出 = Intent IR**：只包含指标名、维度名、过滤值——全部来自已注册列表
2. **L3 代码查表**：`get_binding_by_metric_name(metric_name)` 纯函数映射到物理列
3. **JOIN 白名单**：`JOIN_WHITELIST` 硬编码——LLM 无权选择 JOIN 路径
4. **最坏情况分析**：LLM 幻觉（输出错误指标名）→ 查表失败 → BLOCKED（拒绝执行）

#### 与 Text2SQL Agent 的关键差异

| 安全维度 | Text2SQL Agent | Data Dev Agent |
|---------|---------------|----------------|
| LLM 输出层 | L1 + L2（含 SQLPlan） | 仅 L2（Intent） |
| LLM 接触表名 | 是——从 available_tables 中选择 | 否——代码查 ColumnBindingTable |
| LLM 构造 JOIN | 是——从 join_whitelist 中选并写 ON | 否——代码直接使用 JoinPath |
| 安全保障 | Prompt 约束 + 事后校验兜底 | 代码隔离——LLM 根本看不到物理层 |
| 幻觉影响 | 可能选错表（靠校验拦截） | 最多选错指标名（查表失败 → BLOCKED） |
```

- [ ] **Step 2: 更新版本头和变更记录**

在文档版本头中新增：
```
| 安全审计 P0-P2 全部修复 | 2026-06-14 |
| LLM 防火墙专题（§3.13） | 2026-06-15 |
| 架构审查与方向校准 | 2026-06-15 |
```

在变更记录中新增 v2.6 条目。

- [ ] **Step 3: 提交**

```bash
cd "D:\Program Files\gitvscode"
git add "TianShu Data Dev Agent/"
git commit -m "docs: Obsidian 架构设计文档新增 §3.13 LLM 防火墙专题"
git push origin main
```
